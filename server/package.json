{
  "name": "coverloop-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "//": [
    "express  - HTTP server + routing (@types/express for the typings)",
    "sharp    - image fetch-decode/resize/composite (ships its own types)",
    "gifenc   - palette quantization + GIF encoding (no official types; see src/types.ts)",
    "tsx      - runs TypeScript directly, no compile step: dev AND start use it",
    "typecheck script: types are checked by `npm run typecheck` (tsc --noEmit),",
    "not at runtime - run it before committing (or wire it into CI later)."
  ],
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.19.0",
    "sharp": "^0.33.0",
    "gifenc": "^1.0.3"
  },
  "devDependencies": {
    "@types/express": "^4.17.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
